# 2026-03-17 开发记录 (v23版本)

## 需求背景

用户目标：
1) 从偏好设置页面移除"后台保活"板块（电池优化、应用详情快捷按钮）。
2) 保留后台保活功能（KeepAliveReceiver 相关逻辑）。
3) 简化设置页面 UI，减少用户困惑。

**重要说明**：移除的只是 UI 入口，不影响 ADB/Root 后台保活功能。真正的保活逻辑通过以下方式独立运行：
- `KeepAliveReceiver` 每 30 分钟通过 `AlarmManager` 触发
- `AccessibilityGuardian.restoreIfNeeded()` 使用 `PrivilegeManager` 执行 shell 命令恢复无障碍服务
- 完全不依赖 UI

---

## 一、本次实现范围

### 1) 移除偏好设置页"后台保活"板块

**修改文件**：
- `app/src/main/java/com/antgskds/calendarassistant/ui/page_display/settings/PreferenceSettingsPage.kt`

**原内容（已移除）**：
```kotlin
// ================== 后台保活板块 ==================
Text("后台保活", style = sectionTitleStyle)
Card(
    modifier = Modifier.fillMaxWidth(),
    shape = RoundedCornerShape(16.dp),
    colors = CardDefaults.cardColors(containerColor = MaterialTheme.colorScheme.surfaceContainerLow),
    elevation = CardDefaults.cardElevation(defaultElevation = 0.dp)
) {
    Column(modifier = Modifier.padding(16.dp)) {
        Text(
            text = "建议开启自启动与电池白名单，避免系统清理后台影响提醒与识屏。",
            style = cardSubtitleStyle
        )
        Spacer(modifier = Modifier.height(12.dp))
        Row(
            modifier = Modifier.fillMaxWidth(),
            horizontalArrangement = Arrangement.spacedBy(12.dp)
        ) {
            OutlinedButton(
                onClick = {
                    try {
                        val intent = Intent(Settings.ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS)
                        context.startActivity(intent)
                    } catch (e: Exception) {
                        try {
                            context.startActivity(Intent(Settings.ACTION_SETTINGS))
                        } catch (_: Exception) {
                        }
                    }
                },
                modifier = Modifier.weight(1f)
            ) {
                Text("电池优化")
            }
            OutlinedButton(
                onClick = {
                    try {
                        val intent = Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS).apply {
                            data = Uri.fromParts("package", context.packageName, null)
                        }
                        context.startActivity(intent)
                    } catch (e: Exception) {
                        try {
                            context.startActivity(Intent(Settings.ACTION_SETTINGS))
                        } catch (_: Exception) {
                        }
                    }
                },
                modifier = Modifier.weight(1f)
            ) {
                Text("应用详情")
            }
        }
    }
}
```

### 2) 保留的后台保活功能

以下内容**保持不变**：

| 文件 | 行号 | 作用 |
|------|------|------|
| `KeepAliveReceiver.kt` | 16-64 | 保活接收器，每 30 分钟检查一次 |
| `App.kt` | 70 | 应用启动时调用 `KeepAliveReceiver.schedule(this)` |
| `BootReceiver.kt` | 26 | 开机后调用 `KeepAliveReceiver.schedule(context)` |
| `AndroidManifest.xml` | - | KeepAliveReceiver 声明 |

---

## 二、验证方案

1. ✅ 编译项目确保无错误
2. ✅ 打开偏好设置页面，确认"后台保活"板块已移除
3. ✅ 确认其他设置板块（显示、通知、AI、日程、截图）正常显示
4. ✅ 后台保活功能仍正常运行（每 30 分钟检查一次）

---

## 三、设置页面最终结构

移除后的板块顺序：
1. **显示** - 界面大小、显示明日日程
2. **通知** - 每日日程提醒、实况胶囊通知、取件码聚合、日程提前提醒
3. **AI** - 使用多模态AI、关闭思考
4. **日程** - 日历同步、同步重复日程、自动归档、智能推荐
5. **截图** - 截图延迟

---

## 四、本次弹窗样式升级（FloatingActionCard）

目标：将常见确认类弹窗统一为底部悬浮卡片，并保持遮罩与统一交互。

### 1) 新增通用组件

**新增文件**：
- `app/src/main/java/com/antgskds/calendarassistant/ui/components/FloatingActionCard.kt`

**要点**：
- 支持 `isLoading` 与 `allowDismissWhileLoading`，用于加载中仍可取消。

### 2) 已替换的弹窗

- `app/src/main/java/com/antgskds/calendarassistant/ui/page_display/settings/ArchivesPage.kt`
  - 归档清空确认 → FloatingActionCard + 遮罩 + 破坏性按钮
- `app/src/main/java/com/antgskds/calendarassistant/MainActivity.kt`
  - Prompt 更新 / APP 发生异常 / 异常数据已清除 → FloatingActionCard + 统一遮罩 + 优先级控制（崩溃 > 清理 > Prompt）
- `app/src/main/java/com/antgskds/calendarassistant/ui/page_display/settings/PreferenceSettingsPage.kt`
  - 需要日历权限 → FloatingActionCard + 遮罩
- `app/src/main/java/com/antgskds/calendarassistant/ui/page_display/HomePage.kt`
  - 正在识别 → FloatingActionCard（loading）+ 取消识别

### 3) 识别取消逻辑

- `HomePage` 使用 `imageImportJob` 追踪识别任务。
- 点击“取消”会中止识别，不弹错误提示。

### 4) 保留未替换的弹窗

- 滚轮选择器、复杂表单、导入外部课表、二维码放大等仍保留原 Dialog。

---

## 五、验证建议

1. ✅ 编译通过
2. ✅ 归档清空弹窗：遮罩、按钮颜色、关闭逻辑
3. ✅ Prompt/崩溃/清理弹窗：显示优先级与遮罩点击
4. ✅ 日历权限弹窗：授权流程正常
5. ✅ 正在识别弹窗：取消可中止识别且无错误提示
