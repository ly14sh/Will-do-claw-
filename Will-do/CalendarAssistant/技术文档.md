# Will do 智能日历助手 - 完整技术文档

## 文档信息

| 属性 | 值 |
|------|-----|
| 文档版本 | v3.1 |
| 项目版本 | 1.2.1 |
| 更新日期 | 2026-03-13 |
| 文档类型 | 完整技术文档 + 用户手册 |

---

# 第一部分：项目概述

## 1.1 项目简介

Will do（智能日历助手）是一款基于 Android 平台的智能日程管理应用，采用 Jetpack Compose 构建现代化用户界面，应用 MVVM 架构模式实现清晰的数据分层和模块化设计。项目经过多个版本的迭代优化，已形成一套完整的日程管理解决方案。

该应用的核心设计理念是「智能、简洁、实用」，通过 AI 技术和系统深度集成，为用户提供从日程识别创建到提醒通知的完整闭环体验。无论是课程表管理、日常会议安排，还是取件取餐提醒、交通出行通知，应用都能提供智能化的处理方案。

项目名称 "Will do" 体现了积极主动的生活态度，寓意帮助用户更好地规划和管理时间，让每一项待办事项都能得到妥善处理。应用通过简洁直观的界面设计和智能化的功能实现，让日程管理变得轻松高效。

## 1.2 版本信息

### 当前版本规格

| 项目 | 版本/值 |
|------|---------|
| 应用版本名称 | 1.2.1 |
| 应用版本号 | 10 |
| 最低支持 SDK | 26 (Android 8.0) |
| 目标 SDK | 36 (Android 16) |
| 编译 SDK | 36 |
| Kotlin 版本 | 2.0.21 |
| AGP 版本 | 8.13.2 |
| Compose BOM | 2024.09.00 |

### 版本兼容性说明

Android 8.0（API 26）是应用的最低支持版本，这意味着应用可以在绝大多数活跃设备上运行。目标 SDK 36 表明应用已针对 Android 16 进行优化，充分利用了最新平台特性，包括实况胶囊（Live Activity）等功能。

截图识别功能需要 Android 11（API 30）或更高版本，因为该功能依赖于截屏 API。实况胶囊功能则需要 Android 14+ 或 Flyme 系统支持。这些功能在使用前会进行版本检查，不支持的设备会自动隐藏相关入口。

### 版本历史

| 版本 | 更新日期 | 主要更新内容 |
|------|---------|-------------|
| v1.2.1 | 2026-03-13 | OCR 胶囊接入、多模态 AI、作息表锚点修复、重复日程同步保护、异常日志 |
| v1.2.0 | 2026-03-10 | 取件码聚合模式、网速胶囊 |
| v1.1.0 | 2026-03-08 | 悬浮窗功能增强、智能标题显示 |
| v1.0.0 | 2026-03-05 | 初始稳定版本 |

## 1.3 技术栈详解

### 核心框架与技术

| 类别 | 技术选型 | 版本 |
|------|---------|------|
| UI 框架 | Jetpack Compose | BOM 2024.09.00 |
| UI 组件库 | Material Design 3 | 1.5.4 |
| 状态管理 | ViewModel + StateFlow | 2.10.0 |
| 页面导航 | Navigation Compose | 2.8.0 |
| 网络请求 | Ktor Client | 2.3.7 |
| 文字识别 | ML Kit Text Recognition | 16.0.1 (Chinese) |
| 序列化 | Kotlinx Serialization | 1.6.0 |
| 协程支持 | Kotlinx Coroutines | 1.7.3 |
| Java 兼容 | Desugar JDK Libs | 2.0.4 |

### 技术选型说明

**Jetpack Compose** 是 Google 推荐的现代 Android UI 工具包，它使用声明式编程范式，允许开发者以更直观的方式构建 UI。相比传统的 XML 布局方式，Compose 提供了更强大的动画支持、更灵活的布局系统，以及更简洁的代码风格。在本项目中，100% 的 UI 都采用 Compose 实现，包括复杂的课表视图、手势交互和动画效果。

**Ktor Client** 是一个轻量级的 Kotlin 异步 HTTP 客户端，被选用于 AI API 的调用。相比其他网络库，Ktor 提供了更好的 Kotlin 协程支持和更简洁的 API 设计，非常适合处理 AI 请求这类需要异步处理的场景。Ktor 的内容协商功能与 Kotlinx Serialization 无缝集成，可以自动处理 JSON 的序列化与反序列化。

**ML Kit Text Recognition** 是 Google 提供的机器学习文字识别库，专门针对移动设备进行了优化。支持中文识别是选择该库的关键原因，因为在日程识别场景中，中文文字的提取是核心需求。ML Kit 可以在设备本地完成识别，无需联网，保护用户隐私的同时也提高了响应速度。

**Kotlinx Serialization** 是 Kotlin 官方提供的多平台序列化库，支持 Kotlin 数据类的自动序列化。相比 Gson 等传统 JSON 库，Serialization 提供了更强的类型安全性和更简洁的使用方式，特别适合与 Kotlin 的数据类（data class）配合使用。

### 架构模式：MVVM + Repository

项目采用经典的 MVVM（Model-View-ViewModel）架构，并在此基础上引入了 Repository 模式进行数据抽象。这种架构的优势在于：

- **关注点分离**：UI 层只负责界面展示，业务逻辑集中在 ViewModel 中，数据操作通过 Repository 统一管理
- **响应式数据流**：使用 Kotlin StateFlow 实现数据的响应式更新，UI 自动响应数据变化
- **单例 Repository**：AppRepository 采用单例模式，确保整个应用共享同一数据源，避免数据不一致问题
- **协程支持**：所有耗时操作（网络请求、文件读写）都运行在协程中，不会阻塞主线程

## 1.4 核心功能一览

Will do 应用提供了丰富的日程管理功能，本节将对各项核心功能进行概览性介绍，后续章节会详细展开说明。

### 智能日程识别

应用支持三种智能识别方式：截图识别、文字输入识别、图片导入识别。截图识别利用无障碍服务获取屏幕内容，结合 ML Kit 进行 OCR 文字提取，再通过 AI 解析生成结构化日程；文字输入识别直接解析自然语言描述；图片导入识别支持从相册选择图片进行 OCR 解析。

AI 识别默认采用双 Prompt 并发模式：Schedule Prompt 负责识别普通日程、会议、交通出行等信息，Pickup Prompt 专注于取件码、取餐码等临时事件的识别。两个请求并发执行，通过去重逻辑合并结果，既提高了识别速度，又保证了各类信息的完整性。当开启多模态 AI 时，截图识别会切换为图片直传 + 单一 unified prompt，跳过 OCR 双通道。

**v1.2.1 更新**：新增 OCR 进度/结果胶囊与多模态 AI 开关；优化车牌号格式与取件/取餐图标。

### 课程表管理系统

课程表管理是应用的另一核心功能。用户可以添加、编辑、删除课程，支持单双周课程、排除特定日期、临时调课等复杂场景。课程数据以 JSON 格式存储在本地，与普通日程分离管理。课程在运行时会被转换为虚拟日程显示在时间轴上，但不会污染系统日历。

应用支持从外部导入课程表数据，目前兼容醒课表的数据格式。导入功能会自动解析课程信息，包括课程名称、上课地点、授课教师、节次时间等，并支持学期的周次计算。

**v1.2.1 更新**：作息表编辑器完全解耦，支持早/中/晚三段独立配置，每节课时长可独立调整。

### 实况胶囊通知

实况胶囊（Live Activity）是 Android 14 引入的新特性，可以在锁屏界面等系统区域显示实时更新的通知内容。应用利用这一特性，在日程即将开始时显示胶囊通知，包含日程标题、剩余时间、快捷操作按钮等信息。

除了原生 Android 实况胶囊，应用还适配了 Flyme 系统的实况通知功能。Flyme 是国内定制系统中最早支持类似功能的厂商，其胶囊实现方式与原生略有不同，应用通过 RemoteViews 自定义布局来兼容这两种显示方式。

胶囊状态管理器（CapsuleStateManager）是整个胶囊功能的核心，它统一计算和管理所有需要显示胶囊的日程，包括普通日程、课程和取件码。管理器采用主动唤醒模式，当检测到需要显示胶囊时会自动启动前台服务。

**v1.2.1 更新**：修复已完成事件错误唤醒问题，已完成的事件不再显示胶囊通知；优化网速胶囊显示；聚合取件胶囊支持批量"已取"操作。

### 悬浮窗功能

悬浮窗是 v1.1 版本新增的重要功能，允许用户通过长按音量+键呼出悬浮日程窗口。悬浮窗以系统悬浮窗的形式存在，可以覆盖在其他应用之上显示，非常适合在需要快速查看或操作日程时使用。

悬浮窗提供了智能标题显示功能：根据日程类型动态展示关键信息。火车票会显示检票口或座位号，网约车会显示车牌号，取件码会直接显示取件号码。悬浮窗还支持左滑操作，用户可以通过滑动完成已取件、已检票等快捷操作。

**v1.2.0 更新**：修复网约车动态标题显示错误，现在正确显示车牌号而非车型。

### 日历同步

应用支持与系统日历进行双向同步：本地变更会写入系统日历，系统日历变更会通过内容观察者与定时同步回流到应用。同步功能采用去重机制，避免重复创建相同日程；重复日程同步为 Beta，可选开启并仅同步 ±30 天实例，且有上限保护。

日历同步管理器（CalendarSyncManager）负责处理所有同步相关的逻辑，包括日历账户的查询、事件的创建与更新、冲突解决策略等。同步操作运行在后台协程中，不会影响应用的响应速度。

### 云端 Prompt 更新

v1.2.1 版本新增云端 Prompt 更新能力，应用可以自动检查并更新 AI 识别规则。用户可在设置中开启此功能，当云端有新版本时，应用会弹出对话框让用户自主决定是否更新。这一机制确保 AI 识别能力能够持续优化，而无需更新整个应用。

### 其他功能

除了上述核心功能外，应用还提供了多项实用特性：每日日程汇总通知（在早间和晚间推送当日日程概要）、智能推荐（基于历史数据推荐合适的日程安排）、自动归档（过期日程自动归档以减少干扰）、网速胶囊（在胶囊中显示当前网络速度）、异常日志与轻量 ANR 监测等。这些功能大多可以通过设置进行开关控制，用户可以根据自己的需求进行定制。

---

# 第二部分：架构设计

## 2.1 整体架构概览

Will do 应用采用分层架构设计，从上到下依次为 UI 层、业务逻辑层、数据层和服务层。这种分层方式遵循了软件工程的关注点分离原则，使得各层职责明确，便于维护和扩展。

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              UI Layer (ui/)                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Pages: HomePage, SchedulePage, SettingsPage, FloatingWindow       │   │
│  │  Components: SwipeableEventItem, SettingsSidebar, UniversalToast   │   │
│  │  ViewModels: MainViewModel, SettingsViewModel                       │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Business Logic Layer (core/)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ AI Module    │  │ Calendar     │  │ Capsule      │  │ Course       │   │
│  │ Recognition  │  │ Manager       │  │ StateManager │  │ Manager      │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                     │
│  │ Transport    │  │ Pickup       │  │ Prompt       │                     │
│  │ Utils        │  │ Utils        │  │ Updater      │                     │
│  └──────────────┘  └──────────────┘  └──────────────┘                     │
└─────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            Data Layer (data/)                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  AppRepository (Singleton)                                         │   │
│  │  - events: StateFlow<List<MyEvent>>                                 │   │
│  │  - courses: StateFlow<List<Course>>                                │   │
│  │  - settings: StateFlow<MySettings>                                  │   │
│  │  - capsuleStateManager: CapsuleStateManager                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ EventJson    │  │ CourseJson   │  │ Settings     │  │ Archive      │   │
│  │ DataSource   │  │ DataSource   │  │ DataSource   │  │ DataSource   │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Service Layer (service/)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ Floating     │  │ Capsule       │  │ Notification │  │ Accessibility│   │
│  │ Service      │  │ Service       │  │ Scheduler    │  │ Service      │   │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                     │
│  │ Alarm        │  │ Boot         │  │ Daily        │                     │
│  │ Receiver     │  │ Receiver     │  │ Summary      │                     │
│  └──────────────┘  └──────────────┘  └──────────────┘                     │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 各层职责说明

**UI 层** 负责所有用户可见的内容，包括页面布局、组件渲染和用户交互响应。UI 层不直接处理业务逻辑，而是通过观察 ViewModel 中的 StateFlow 来获取数据变化，并据此更新界面。所有用户操作都会被转发到 ViewModel 进行处理。

**业务逻辑层（core）** 包含了应用的核心功能实现，如 AI 识别处理、日历操作封装、胶囊状态计算、课程管理等，同时负责稳定性监测与异常日志记录（CrashHandler/AnrMonitor/ExceptionLogStore）。这一层是应用的大脑，负责所有的决策和计算逻辑。业务逻辑层通过调用数据层接口获取数据，处理后再将结果暴露给 UI 层。

**数据层** 负责所有数据的持久化和读取操作。数据层采用 Repository 模式，对外提供统一的数据访问接口，屏蔽了底层存储细节。数据层内部使用 JSON 文件作为存储介质，通过多个 DataSource 类分别管理不同类型的数据。

**服务层** 处理需要在后台长期运行的任务，如悬浮窗显示、胶囊通知、闹钟提醒等。服务层通过系统服务（Service）和广播接收器（BroadcastReceiver）与操作系统进行交互，实现各种系统级别的功能。

## 2.2 MVVM + Repository 架构实现

### ViewModel 与 StateFlow

项目中主要使用两个 ViewModel：MainViewModel 负责主页相关的业务逻辑，SettingsViewModel 负责设置页面的逻辑。每个 ViewModel 都通过 StateFlow 向 UI 暴露状态数据。

```kotlin
class MainViewModel(
    private val repository: AppRepository
) : ViewModel() {
    
    // 暴露给 UI 的状态数据（只读）
    val selectedDate: StateFlow<LocalDate> = _selectedDate.asStateFlow()
    val eventsForSelectedDate: StateFlow<List<MyEvent>> = _eventsForSelectedDate.asStateFlow()
    val coursesForSelectedDate: StateFlow<List<Course>> = _coursesForSelectedDate.asStateFlow()
    
    // UI 事件处理
    fun onDateSelected(date: LocalDate) {
        _selectedDate.value = date
    }
    
    fun onEventCompleted(eventId: String) {
        viewModelScope.launch {
            repository.completeScheduleEvent(eventId)
        }
    }
}
```

StateFlow 是 Kotlin 协程库提供的响应式数据容器，与 LiveData 类似但更加强大。StateFlow 始终持有最新的数据值，当数据变化时会自动通知所有观察者。与 LiveData 不同的是，StateFlow 不需要.lifecycleScope 来自动取消订阅，也不局限于在 Compose 中使用。

### Repository 单例模式

AppRepository 是整个应用的数据枢纽，采用单例模式确保全局唯一：

```kotlin
class AppRepository private constructor(private val context: Context) {
    
    // 内部可变的状态
    private val _events = MutableStateFlow<List<MyEvent>>(emptyList())
    private val _courses = MutableStateFlow<List<Course>>(emptyList())
    private val _settings = MutableStateFlow(MySettings())
    
    // 暴露给外部的只读状态
    val events: StateFlow<List<MyEvent>> = _events.asStateFlow()
    val courses: StateFlow<List<Course>> = _courses.asStateFlow()
    val settings: StateFlow<MySettings> = _settings.asStateFlow()
    
    // 胶囊状态管理器 - 直接初始化
    val capsuleStateManager: CapsuleStateManager = 
        CapsuleStateManager(this, scope, context.applicationContext)
    
    companion object {
        @Volatile
        private var INSTANCE: AppRepository? = null
        
        fun getInstance(context: Context): AppRepository {
            return INSTANCE ?: synchronized(this) {
                INSTANCE ?: AppRepository(context.applicationContext).also { INSTANCE = it }
            }
        }
    }
}
```

单例模式保证了整个应用使用同一份数据，避免了因多个数据实例导致的同步问题。同时，所有数据操作都通过 Repository 进行，使得数据流清晰可控，便于调试和优化。

## 2.3 胶囊状态管理架构

胶囊状态管理是应用的核心创新点之一，它实现了一种「主动唤醒」的设计模式：将胶囊显示的计算逻辑与前台服务分离，由 CapsuleStateManager 统一计算状态，当状态变为 Active 时自动启动 CapsuleService。

### CapsuleStateManager 设计

```kotlin
class CapsuleStateManager(
    private val repository: AppRepository,
    private val appScope: CoroutineScope,
    private val context: Context
) {
    
    // 胶囊 UI 状态 - StateFlow
    val uiState: StateFlow<CapsuleUiState> = createCapsuleStateFlow()
    
    init {
        // 主动唤醒机制：监听 uiState，当变为 Active 时启动 Service
        startServiceWakeup()
    }
    
    // 合并多个数据源：events + courses + settings + ticker + ocr + networkSpeed
    private fun createCapsuleStateFlow(): StateFlow<CapsuleUiState> {
        val tickerTrigger = MutableStateFlow(System.currentTimeMillis())
        
        // 定时器：每 10 秒更新一次（快速检测过期）
        appScope.launch {
            while (true) {
                delay(10_000)
                tickerTrigger.value = System.currentTimeMillis()
            }
        }
        
        val baseCombine = combine(
            repository.events,
            repository.courses,
            repository.settings,
            tickerTrigger,
            forceRefreshTrigger
        ) { events, courses, settings, _, _ ->
            Triple(events, courses, settings)
        }

        return combine(baseCombine, networkSpeedState, ocrCapsuleState) { (events, courses, settings), networkSpeed, ocrCapsule ->
            computeCapsuleState(events, courses, settings, networkSpeed, ocrCapsule)
        }.stateIn(...)
    }
}
```

这种设计的核心优势在于：

1. **计算与显示分离**：CapsuleStateManager 只负责状态计算，CapsuleService 只负责界面显示，职责清晰
2. **响应式更新**：利用 combine 操作符将多个数据源合并，任何一个数据源变化都会触发重新计算
3. **主动唤醒**：通过 collect 监听状态变化，当需要显示胶囊时自动启动服务，不再需要时自动停止
4. **优先级策略**：OCR 胶囊优先于网速胶囊与事件胶囊，确保识别状态可见
5. **强制刷新**：提供 forceRefresh() 方法，可以在特定操作后立即触发状态更新
6. **已完成过滤**：已完成的事件（isCompleted=true）不再显示在胶囊中

### CapsuleService Dumb Service 模式

CapsuleService 采用「Dumb Service」（哑服务）模式运行，它本身不进行任何业务逻辑计算，只是简单地监听状态变化并更新通知：

```kotlin
class CapsuleService : Service() {
    
    private val repository by lazy { AppRepository.getInstance(applicationContext) }
    private val capsuleManager by lazy { 
        CapsuleStateManager(repository, serviceScope, applicationContext) 
    }
    
    override fun onCreate() {
        super.onCreate()
        // 监听胶囊状态，状态变化时更新通知
        serviceScope.launch {
            capsuleManager.uiState.collect { state ->
                when (state) {
                    is CapsuleUiState.Active -> showCapsuleNotification(state)
                    is CapsuleUiState.None -> cancelAllNotifications()
                }
            }
        }
    }
}
```

这种模式避免了服务本身维护复杂状态，所有状态都由 CapsuleStateManager 统一管理，服务只是一个执行者。

### 聚合取件胶囊模式

v1.2.0 版本引入了取件码聚合模式，当用户开启该功能且同一天有多个活跃取件码时，胶囊不再分别显示，而是聚合为一个统一的胶囊。聚合胶囊会显示取件码数量，点击可执行批量"已取"操作。

```kotlin
val aggregateMode = settings.isPickupAggregationEnabled && pickupEvents.size > 1

if (aggregateMode) {
    // 聚合模式：只创建聚合胶囊
    capsules.add(createCapsuleItem(
        id = AGGREGATE_PICKUP_ID,
        notifId = AGGREGATE_NOTIF_ID,
        type = CapsuleService.TYPE_PICKUP,
        ...
    ))
} else {
    // 非聚合模式：创建独立胶囊
    pickupEvents.forEach { event ->
        capsules.add(createCapsuleItem(...))
    }
}
```

---

# 第三部分：目录结构

```
F:\CalendarAssistant\CalendarAssistant\
├── app/                                    # 主应用模块
│   ├── src/main/
│   │   ├── java/com/antgskds/calendarassistant/
│   │   │   ├── App.kt                     # Application 单例
│   │   │   ├── MainActivity.kt            # 主入口 Activity
│   │   │   ├── core/                      # 核心业务逻辑层
│   │   │   │   ├── ai/                    # AI 相关功能
│   │   │   │   │   ├── AiModelConfig.kt   # AI 模型配置
│   │   │   │   │   ├── AiPrompts.kt       # AI 提示词模板
│   │   │   │   │   ├── ApiModelProvider.kt    # AI API 提供者
│   │   │   │   │   ├── RecognitionProcessor.kt # 识别处理器
│   │   │   │   │   └── PromptUpdater.kt    # 云端 Prompt 更新器
│   │   │   │   ├── calendar/              # 日历管理
│   │   │   │   │   ├── CalendarManager.kt         # 日历管理器
│   │   │   │   │   ├── CalendarSyncManager.kt    # 日历同步管理器
│   │   │   │   │   ├── CalendarEventMapper.kt    # 事件映射
│   │   │   │   │   ├── CalendarContentObserver.kt # 内容观察者
│   │   │   │   │   └── CalendarPermissionHelper.kt # 权限助手
│   │   │   │   ├── capsule/               # 胶囊状态管理
│   │   │   │   │   └── CapsuleStateManager.kt # 胶囊状态管理器
│   │   │   │   ├── course/                # 课程管理
│   │   │   │   │   ├── CourseManager.kt   # 课程管理器
│   │   │   │   │   └── TimeTableLayoutUtils.kt # 作息表布局工具
│   │   │   │   ├── importer/              # 课程导入
│   │   │   │   │   ├── ICourseImporter.kt         # 导入接口
│   │   │   │   │   ├── ImportModels.kt            # 导入模型
│   │   │   │   │   └── WakeUpCourseImporter.kt   # 醒课表导入器
│   │   │   │   ├── service/               # 服务工具
│   │   │   │   │   ├── image/             # 图片处理
│   │   │   │   │   │   └── ImagePickHandleActivity.kt
│   │   │   │   │   └── shortcut/          # 快捷方式
│   │   │   │   │       └── ShortcutHandleActivity.kt
│   │   │   │   └── util/                  # 工具类
│   │   │   │       ├── AnrMonitor.kt      # ANR 监测
│   │   │   │       ├── CrashHandler.kt    # 崩溃捕获
│   │   │   │       ├── DataSanitizer.kt   # 数据清洗
│   │   │   │       ├── DateCalculator.kt  # 日期计算工具
│   │   │   │       ├── DensityConfigManager.kt # 屏幕密度配置
│   │   │   │       ├── EventDeduplicator.kt     # 事件去重工具
│   │   │   │       ├── ExceptionLogStore.kt # 异常日志存储
│   │   │   │       ├── FlymeUtils.kt      # Flyme 系统工具
│   │   │   │       ├── ImageImportUtils.kt # 图片导入工具
│   │   │   │       ├── LayoutAnalyzer.kt  # OCR 布局分析
│   │   │   │       ├── LunarCalendarUtils.kt # 农历工具
│   │   │   │       ├── PickupUtils.kt     # 取件码工具
│   │   │   │       ├── ScreenMetrics.kt   # 屏幕参数
│   │   │   │       └── TransportUtils.kt   # 火车/网约车解析工具
│   │   │   ├── data/                      # 数据层
│   │   │   │   ├── model/                 # 数据模型
│   │   │   │   │   ├── CalendarEventData.kt # 日历事件数据
│   │   │   │   │   ├── Course.kt          # 课程模型
│   │   │   │   │   ├── EventFingerprint.kt    # 事件指纹
│   │   │   │   │   ├── ImportResult.kt    # 导入结果
│   │   │   │   │   ├── ModelPayload.kt    # 模型请求数据
│   │   │   │   │   ├── MyEvent.kt         # 用户事件模型
│   │   │   │   │   ├── MySettings.kt      # 设置模型
│   │   │   │   │   ├── RemotePrompts.kt   # 云端 Prompt 模型
│   │   │   │   │   ├── SyncData.kt        # 同步数据模型
│   │   │   │   │   ├── TimeNode.kt        # 时间节点模型
│   │   │   │   │   ├── external/          # 外部数据模型
│   │   │   │   │   │   └── wakeup/        # 醒课表DTO
│   │   │   │   │   │       └── WakeUpDTOs.kt
│   │   │   │   │   └── serializers/       # 序列化器
│   │   │   │   │       ├── ColorSerializer.kt # 颜色序列化
│   │   │   │   │       └── LocalDateSerializer.kt # 日期序列化
│   │   │   │   ├── repository/            # 仓储层
│   │   │   │   │   └── AppRepository.kt   # 应用仓储（单例）
│   │   │   │   ├── source/                # 数据源
│   │   │   │   │   ├── ArchiveJsonDataSource.kt # 归档数据源
│   │   │   │   │   ├── CourseJsonDataSource.kt # 课程数据源
│   │   │   │   │   ├── EventJsonDataSource.kt  # 事件数据源
│   │   │   │   │   ├── SettingsDataSource.kt   # 设置数据源
│   │   │   │   │   └── SyncJsonDataSource.kt   # 同步数据源
│   │   │   │   └── state/                 # 状态定义
│   │   │   │       └── CapsuleUiState.kt  # 胶囊UI状态
│   │   │   ├── service/                   # 服务层
│   │   │   │   ├── floating/              # 悬浮窗服务
│   │   │   │   │   └── FloatingScheduleService.kt # 悬浮窗服务
│   │   │   │   ├── capsule/               # 胶囊服务
│   │   │   │   │   ├── CapsuleService.kt  # 胶囊服务（Dumb Service）
│   │   │   │   │   ├── CapsuleUiUtils.kt  # 胶囊 UI 工具
│   │   │   │   │   ├── CapsuleDisplayModel.kt # 胶囊显示模型
│   │   │   │   │   ├── CapsuleMessageComposer.kt # 胶囊消息组装
│   │   │   │   │   ├── IconUtils.kt       # 图标映射工具
│   │   │   │   │   ├── NetworkSpeedMonitor.kt # 网速监控
│   │   │   │   │   └── provider/          # 胶囊提供者
│   │   │   │   │       ├── FlymeCapsuleProvider.kt # Flyme 胶囊
│   │   │   │   │       ├── ICapsuleProvider.kt     # 胶囊接口
│   │   │   │   │       └── NativeCapsuleProvider.kt # 原生胶囊
│   │   │   │   ├── notification/          # 通知服务
│   │   │   │   │   └── NotificationScheduler.kt # 通知调度器
│   │   │   │   ├── receiver/              # 广播接收器
│   │   │   │   │   ├── AlarmReceiver.kt   # 闹钟接收器
│   │   │   │   │   ├── BootReceiver.kt    # 开机启动接收器
│   │   │   │   │   ├── DailySummaryReceiver.kt # 每日摘要接收器
│   │   │   │   │   └── EventActionReceiver.kt    # 事件操作接收器
│   │   │   │   ├── accessibility/         # 无障碍服务
│   │   │   │   │   └── TextAccessibilityService.kt # 文本无障碍服务
│   │   │   │   └── tile/                  # 快捷服务
│   │   │   │       └── CaptureTileService.kt # 截图快捷服务
│   │   │   ├── ui/                        # 用户界面
│   │   │   │   ├── analyzer/              # 智能分析
│   │   │   │   │   └── ScheduleRecommendationAnalyzer.kt # 日程推荐分析器
│   │   │   │   ├── components/            # 通用组件
│   │   │   │   │   ├── CommonSettings.kt  # 通用设置组件
│   │   │   │   │   ├── SettingsSidebar.kt # 设置侧边栏
│   │   │   │   │   └── UniversalToast.kt  # 通用 Toast 组件
│   │   │   │   ├── dialogs/               # 对话框组件
│   │   │   │   │   ├── AddEventDialog.kt  # 添加事件对话框
│   │   │   │   │   └── CourseManagementDialog.kt # 课程管理对话框
│   │   │   │   ├── event_display/         # 事件显示组件
│   │   │   │   │   └── SwipeableEventItem.kt # 可滑动事件项
│   │   │   │   ├── layout/                # 自定义布局
│   │   │   │   │   └── PushSlideLayout.kt # 侧滑抽屉布局
│   │   │   │   ├── navigation/            # 导航与转场
│   │   │   │   │   └── AppNavTransitions.kt # 转场动画
│   │   │   │   ├── floating/              # 悬浮窗UI
│   │   │   │   │   └── FloatingScheduleScreen.kt # 悬浮窗界面
│   │   │   │   ├── page_display/          # 页面显示
│   │   │   │   │   ├── AllEventsPage.kt   # 所有事件页面
│   │   │   │   │   ├── HomePage.kt        # 主页（今日视图）
│   │   │   │   │   ├── HomeScreen.kt      # 主屏容器
│   │   │   │   │   ├── SchedulePage.kt    # 日程页面
│   │   │   │   │   ├── SettingsDetailScreen.kt # 设置详情容器
│   │   │   │   │   └── settings/          # 设置子页面
│   │   │   │   │       ├── AboutPage.kt           # 关于页面
│   │   │   │   │       ├── AiSettingsPage.kt      # AI 模型设置
│   │   │   │   │       ├── ArchivesPage.kt        # 归档页面
│   │   │   │   │       ├── BackupSettingsPage.kt  # 数据备份设置
│   │   │   │   │       ├── CourseManagerScreen.kt # 课程管理页面
│   │   │   │   │       ├── LaboratoryPage.kt      # 实验室页面
│   │   │   │   │       ├── PreferenceSettingsPage.kt # 偏好设置
│   │   │   │   │       ├── ScheduleSettingsPage.kt  # 课表设置
│   │   │   │   │       ├── ThemeSettingsPage.kt    # 主题设置
│   │   │   │   │       └── TimeTableEditorScreen.kt # 作息时间编辑
│   │   │   │   ├── theme/                 # 主题配置
│   │   │   │   │   ├── Color.kt           # 颜色定义
│   │   │   │   │   ├── Theme.kt           # 主题定义
│   │   │   │   │   ├── ThemeColorGenerator.kt # 主题颜色生成器
│   │   │   │   │   ├── ThemeColorScheme.kt # 主题配色方案
│   │   │   │   │   └── Type.kt            # 字体类型定义
│   │   │   │   └── viewmodel/             # ViewModel
│   │   │   │       ├── MainViewModel.kt   # 主 ViewModel
│   │   │   │       └── SettingsViewModel.kt # 设置 ViewModel
│   │   │   └── materialcolor/             # Material Color 工具库
│   │   │       ├── blend/                 # 颜色混合
│   │   │       ├── contrast/              # 对比度计算
│   │   │       ├── dislike/               # 颜色厌恶分析
│   │   │       ├── dynamiccolor/          # 动态颜色
│   │   │       ├── hct/                   # HCT 色彩空间
│   │   │       ├── palettes/              # 色调调色板
│   │   │       ├── quantize/              # 颜色量化算法
│   │   │       ├── scheme/                # 配色方案
│   │   │       ├── score/                 # 颜色评分
│   │   │       ├── temperature/           # 色温
│   │   │       └── utils/                  # 颜色工具
│   │   │   ├── res/                       # 资源文件
│   │   │   │   ├── drawable/              # 图形资源
│   │   │   │   │   ├── ic_stat_*.xml     # 状态栏图标
│   │   │   │   │   ├── ic_capsule_*.xml  # 胶囊图标
│   │   │   │   │   └── ic_qs_*.xml        # 快捷方式图标
│   │   │   │   ├── layout/                # 布局资源
│   │   │   │   │   ├── notification_live_flyme.xml # Flyme实况通知布局
│   │   │   │   │   └── notification_live_network_speed.xml # 网速胶囊布局
│   │   │   │   ├── mipmap-*/              # 应用图标
│   │   │   │   ├── values/                # 值资源
│   │   │   │   └── xml/                   # XML 配置
│   │   │   └── AndroidManifest.xml        # 应用清单
│   │   └── build.gradle.kts               # 应用级构建配置
│   ├── build.gradle.kts                   # 应用级构建配置
├── gradle/                                # Gradle 包装器
│   └── libs.versions.toml                 # 版本目录
├── build.gradle.kts                       # 项目级构建配置
├── settings.gradle.kts                    # 项目设置
├── gradle.properties                      # Gradle 属性
├── local.properties                       # 本地配置
└── 技术文档.md                            # 本文档
```

---

# 第四部分：核心功能详解

## 4.1 AI 智能识别

AI 智能识别是应用最具特色的功能之一，它能够从截屏或文本中自动提取日程信息。用户无需手动填写繁琐的表单，只需要提供相关信息，AI 就能完成解析和创建。

### 4.1.1 识别流程架构

整个识别流程可以分为以下几个阶段：

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户触发识别                                 │
│  (快捷设置磁贴 / 一键识屏 / 手动输入)                            │
└──────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   TextAccessibilityService                      │
│                   (无障碍服务截屏)                                │
└──────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                 识别路径选择 (useMultimodalAi)                   │
│  ├─ 多模态 AI：图片直传 + unified prompt                          │
│  └─ 文本 AI：ML Kit OCR + 双 Prompt 并发解析                      │
└──────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     结果合并与去重                                 │
│  - 按 title+startTime 分组                                      │
│  - 优先保留 tag=pickup 的结果                                   │
│  - 低信息量日程清洗                                              │
└──────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   本地保存与日历同步                               │
│  - 保存到 events.json                                           │
│  - 同步到系统日历                                                │
└──────────────────────────┬──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     显示结果通知                                  │
│  - 动态状态栏图标                                                │
└─────────────────────────────────────────────────────────────────┘
```

### 4.1.2 双 Prompt 并发模式

AI 识别采用双 Prompt 并发模式是 v1.5 版本的重要优化。在此之前，应用使用单一 Prompt 要求 AI 同时识别所有类型的日程，但这种设计存在两个问题：Prompt 过长导致 AI 响应变慢，且某些边界情况的识别准确率不高。开启多模态 AI 时，识别流程改为 unified prompt，以下双 Prompt 描述适用于文本 AI 模式。

新的设计将识别任务拆分为两个独立的 Prompt：

**Schedule Prompt（普通日程）**

```
你是一个日程识别助手。从以下文本中提取日程信息。

要求：
1. 识别会议、约会、出行等普通日程
2. 如果文本中有火车票信息，格式为【列车】车次|检票口|座位号
3. 如果文本中有网约车信息，格式为【用车】颜色|车型|车牌 (例: "【用车】白色|零跑C10|鄂J·DB7979")
4. 车牌号格式：省份+城市代码·字母数字 (如: 鄂A·BT558、粤B·12345)
5. 只返回普通日程，不要返回取件码信息
6. tag 字段：general=普通日程, train=火车, taxi=网约车

返回 JSON 格式：
{
  "events": [
    {
      "title": "日程标题",
      "description": "详细描述或交通微格式",
      "startTime": "HH:mm",
      "endTime": "HH:mm",
      "tag": "general|train|taxi"
    }
  ]
}
```

**Pickup Prompt（取件码）**

```
你是一个取件码识别助手。从以下文本中提取取件码信息。

要求：
1. 识别快递取件码、取餐码、核销码等信息
2. 快递格式：号码|品牌|位置 (例: "【取件】5566|顺丰|菜鸟驿站")
3. 餐饮格式：号码|品牌|位置 (例: "【取餐】1234|麦当劳|取餐口")
4. tag 字段统一为 pickup
5. 只返回取件码信息，不要返回普通日程

返回 JSON 格式：
{
  "events": [
    {
      "title": "取件码 XXX" 或 "取餐码 XXX",
      "description": "【取件】号码|品牌|位置" 或 "【取餐】号码|品牌|位置",
      "tag": "pickup"
    }
  ]
}
```

### 4.1.3 结果合并与去重

由于 Schedule Prompt 和 Pickup Prompt 是并发执行的，可能会出现重复识别的情况。例如，一条同时包含取件码和会议通知的文本，两个 Prompt 可能都会尝试识别。

去重逻辑基于 title 和 startTime 的组合进行分组，优先保留 tag 为 pickup 的结果：

```kotlin
val finalEvents = allEvents
    .groupBy { "${it.title}|${it.startTime}" }
    .map { (_, events) ->
        events.maxByOrNull { if (it.tag == "pickup") 1 else 0 }!!
    }
```

此外，应用还实现了「低信息量日程清洗」功能：如果一个普通日程的标题只包含取件关键词（如"取件"），且已被 Pickup 结果覆盖，则该日程会被过滤掉，避免重复显示。

### 4.1.4 微格式规范

为了在日程中携带更多结构化信息，应用定义了以下微格式规范：

| 类型 | 格式 | 示例 |
|------|------|------|
| 火车票 | `【列车】车次\|检票口\|座位号` | `【列车】G1024\|8A\|15A` |
| 网约车 | `【用车】颜色\|车型\|车牌` | `【用车】白色\|零跑C10\|鄂J·DB7979` |
| 取件码 | `【取件】号码\|品牌\|位置` | `【取件】5566\|顺丰\|菜鸟驿站` |
| 取餐码 | `【取餐】号码\|品牌\|位置` | `【取餐】1234\|麦当劳\|取餐口` |

这些微格式信息会被胶囊状态管理器解析，用于在胶囊和悬浮窗中显示更直观的动态标题。

### 4.1.5 RecognitionProcessor 实现

图像识别处理器协调整个识别流程：

```kotlin
suspend fun analyzeImage(bitmap: Bitmap, settings: MySettings, context: Context): List<CalendarEventData> {
    if (settings.useMultimodalAi) {
        return analyzeImageWithMultimodal(bitmap, settings, context)
    }

    // 1. ML Kit OCR 提取文字
    val ocrResult = processImageWithMlKit(bitmap)
    val reconstructedText = LayoutAnalyzer.reconstructLayout(...)

    // 2. 并发执行两个 AI 请求
    val scheduleDeferred = async { analyzeSchedule(reconstructedText, settings, context) }
    val pickupDeferred = async { analyzePickup(reconstructedText, settings, context) }
    val scheduleEvents = scheduleDeferred.await()
    val pickupEvents = pickupDeferred.await()

    // 3. 过滤冗余日程
    val refinedScheduleEvents = filterRedundantSchedules(scheduleEvents, pickupEvents)

    // 4. 合并结果并去重
    return mergeAndDeduplicate(refinedScheduleEvents, pickupEvents)
}
```

### 4.1.6 云端 Prompt 更新

v1.2.1 版本新增云端 Prompt 更新能力。应用启动时会静默检查云端是否有新版本的 Prompt，当发现 remote.version > localVersion 时，会弹出对话框让用户选择是否更新。

```kotlin
class PromptUpdater {
    suspend fun checkForUpdates(): RemotePrompts? {
        val remote = fetchRemotePrompts()
        val local = loadLocalPrompts()
        return if (remote.version > local.version) remote else null
    }
    
    fun updatePrompts(remote: RemotePrompts) {
        saveLocalPrompts(remote)
    }
}
```

Prompt 更新机制的设计原则：
- 用户可感知：必须通过对话框让用户确认
- 版本比较：只有新版本才提示更新
- 忽略功能：用户点击"取消"后，同版本不再重复打扰

多模态模式新增 `mm_unified_prompt` 字段，用于图片直传识别的统一 Prompt；当字段缺失时回退到本地默认提示词。

### 4.1.7 动态状态栏图标

AI 识别过程中会根据不同状态显示对应的状态栏图标，提升视觉反馈体验。

| 状态 | 图标 | 用途 |
|------|------|------|
| 识别中 | `ic_stat_scan.xml` (📷) | 显示扫描框图标 |
| 分析完成 | `ic_stat_sparkle.xml` (✨) | 显示星星图标 |
| 添加成功 | `ic_stat_success.xml` (✓) | 显示勾选图标 |
| 快递取件 | `ic_stat_package.xml` (📦) | 快递类取件码 |
| 餐饮取餐 | `ic_stat_food.xml` (🍔) | 餐饮类取件码 |
| 火车出行 | `ic_stat_train.xml` (🚄) | 火车票事件 |
| 打车出行 | `ic_stat_car.xml` (🚗) | 网约车事件 |
| 网速胶囊 | `ic_stat_net.xml` | 网络速度显示 |

### 4.1.8 AI 服务商支持

应用支持多种 AI 服务商：

| 服务商 | 模型名称 | API 地址格式 |
|--------|----------|-------------|
| DeepSeek | deepseek-chat, deepseek-coder, deepseek-reasoner | https://api.deepseek.com/v1/chat/completions |
| OpenAI | gpt-4o-mini, gpt-4o, gpt-3.5-turbo | https://api.openai.com/v1/chat/completions |
| Gemini | gemini-1.5-flash, gemini-1.5-pro | https://generativelanguage.googleapis.com/v1beta/models/... |
| 自定义 | 任意兼容 OpenAI 格式 | 用户自定义 |

多模态识别要求模型支持图片输入（OpenAI 视觉接口或 Gemini 多模态接口）。

## 4.2 课程表管理

课程表是应用的核心功能之一，支持完整的课程管理功能，包括添加、编辑、删除、导入等操作。

### 4.2.1 三段式作息表

v1.2.1 版本对作息表编辑器进行了重大重构，将传统的固定节数模式改为早/中/晚三段式结构，并将布局配置快照持久化到 `timeTableConfigJson`：

```kotlin
data class TimeTableLayoutConfig(
    val morningCount: Int = 4,
    val afternoonCount: Int = 4,
    val nightCount: Int = 4,
    val morningStart: LocalTime = LocalTime.of(8, 0),
    val afternoonStart: LocalTime = LocalTime.of(14, 0),
    val nightStart: LocalTime = LocalTime.of(19, 0),
    val customBreaks: Map<Int, Int> = emptyMap(),
    val customDurations: Map<Int, Int> = emptyMap()
)
```

用户可以分别配置：
- **上午节数**：2-6 节
- **下午节数**：2-6 节
- **晚上节数**：0-4 节
- **节次时长**：30-90 分钟（可按节单独设置）
- **节间休息**：可按节单独设置
- **分段起始时间**：上午/下午/晚上分别独立配置

每节课时长可以独立调整，不再是全局固定值。系统会根据上一节结束时间和课间休息自动推算下一节开始时间。

### 4.2.2 课程数据模型

```kotlin
data class Course(
    val id: String,                      // UUID
    val name: String,                    // 课程名称
    val location: String = "",          // 上课地点
    val teacher: String = "",           // 授课教师
    val color: Color,                    // 课程颜色
    val dayOfWeek: Int,                  // 星期几 (1-7)
    val startNode: Int,                  // 起始节次
    val endNode: Int,                    // 结束节次
    val startWeek: Int,                  // 起始周
    val endWeek: Int,                    // 结束周
    val weekType: Int = 0,               // 0=全周, 1=单周, 2=双周
    val excludedDates: List<String> = emptyList(), // 排除日期 (yyyy-MM-dd)
    val isTemp: Boolean = false,         // 影子课程标记
    val parentCourseId: String? = null   // 父课程关联
)
```

### 4.2.3 课程导入

应用支持从醒课表导入课程数据，导入器会解析外部 JSON 格式并转换为内部 Course 模型：

```kotlin
class WakeUpCourseImporter : ICourseImporter {
    override suspend fun import(data: String, mode: ImportMode): ImportResult
}
```

### 4.2.4 虚拟日程转换

课程在运行时会被转换为虚拟日程（MyEvent）显示在时间轴上，但不会同步到系统日历：

```kotlin
fun Course.toVirtualEvent(date: LocalDate, settings: MySettings): MyEvent {
    val timeNode = TimeTableLayoutUtils.getTimeNode(startNode)
    return MyEvent(
        title = name,
        startDate = date,
        endDate = date,
        startTime = timeNode.startTime,
        endTime = TimeTableLayoutUtils.getTimeNode(endNode).endTime,
        location = location,
        eventType = EventType.COURSE,
        skipCalendarSync = true
    )
}
```

### 4.2.5 影子课程机制

影子课程（Temp Course）是用于处理单次课程调整的机制。当某次课程临时调课、更换教室或取消时，不需要修改主课程设置，而是创建一个影子课程：

- 影子课程的 `isTemp` 标记为 `true`
- 影子课程关联父课程的 `parentCourseId`
- 原主课程将该日期加入 `excludedDates`

这种设计的优势在于：学期结束后清理课程时，只需要删除主课程，影子课程会作为附带清理，无需单独处理。

### 4.2.6 CourseManager 实现

课程管理器负责课程数据与虚拟日程之间的转换：

```kotlin
class CourseManager(
    private val repository: AppRepository
) {
    // 将课程转换为虚拟日程显示
    fun getDailyCourses(date: LocalDate, courses: List<Course>, settings: MySettings): List<MyEvent> {
        val currentWeek = calculateCurrentWeek(settings, date)
        val dayOfWeek = date.dayOfWeek.value
        
        return courses
            .filter { course -> isCourseActive(course, currentWeek, dayOfWeek, date) }
            .map { course -> convertToEvent(course, date) }
    }
    
    private fun isCourseActive(
        course: Course,
        currentWeek: Int,
        dayOfWeek: Int,
        date: LocalDate
    ): Boolean {
        // 1. 周次范围检查
        if (currentWeek !in course.startWeek..course.endWeek) return false
        
        // 2. 单双周检查
        if (!isWeekTypeMatch(course.weekType, currentWeek)) return false
        
        // 3. 星期几检查
        if (course.dayOfWeek != dayOfWeek) return false
        
        // 4. 排除日期检查
        val dateStr = date.format(DateTimeFormatter.ISO_LOCAL_DATE)
        if (dateStr in course.excludedDates) return false
        
        return true
    }
    
    private fun isWeekTypeMatch(weekType: Int, currentWeek: Int): Boolean {
        return when (weekType) {
            0 -> true  // 每周
            1 -> currentWeek % 2 == 1  // 单周
            2 -> currentWeek % 2 == 0  // 双周
            else -> true
        }
    }
    
    private fun convertToEvent(course: Course, date: LocalDate): MyEvent {
        val timeTable = parseTimeTableJson(repository.settings.value.timeTableJson)
        val startNode = course.startNode
        val endNode = course.endNode
        
        return MyEvent(
            id = "course_${course.id}_${date}",
            title = course.name,
            startDate = date,
            endDate = date,
            startTime = timeTable[startNode].startTime,
            endTime = timeTable[endNode].endTime,
            location = course.location,
            description = course.teacher,
            color = course.color,
            eventType = EventType.COURSE
        )
    }
}
```

## 4.3 实况胶囊通知

实况胶囊（Live Activity）是 Android 14 引入的新特性，可以在锁屏界面等系统区域显示实时更新的通知内容。

### 4.3.1 胶囊类型

应用支持多种类型的胶囊：

| 类型 | 说明 | 显示条件 |
|------|------|---------|
| 日程胶囊 | 普通日程/课程 | 即将开始前 X 分钟 |
| 取件码胶囊 | 取件/取餐码 | 取件码有效期间 |
| 聚合取件胶囊 | 多个取件码聚合 | 开启聚合且数量>1 |
| 网速胶囊 | 当前网速 | 实验室开启 |
| OCR 进度胶囊 | 识别进度 | 开启实况胶囊且正在识别 |
| OCR 结果胶囊 | 识别结果 | 开启实况胶囊且识别完成 |

OCR 胶囊具备自动清理与节流策略：进度胶囊默认 2 分钟超时，结果胶囊默认 8 秒展示，更新节流 600ms。

### 4.3.2 胶囊状态计算

胶囊状态由 CapsuleStateManager 统一计算，核心逻辑：

```kotlin
private fun computeCapsuleState(...): CapsuleUiState {
    // 1. OCR 胶囊优先
    if (ocrCapsule != null && settings.isLiveCapsuleEnabled) {
        return CapsuleUiState.Active(listOf(ocrCapsule))
    }

    // 2. 网速胶囊优先（仅在未触发 OCR 胶囊时）
    if (settings.isNetworkSpeedCapsuleEnabled && networkSpeed != null) {
        return CapsuleUiState.Active(listOf(networkSpeedCapsule))
    }
    
    // 3. 检查胶囊开关
    if (!settings.isLiveCapsuleEnabled) {
        return CapsuleUiState.None
    }
    
    // 3. 过滤活跃事件（未完成 + 未过期）
    val activeEvents = allEvents.filter { event ->
        !event.isCompleted && now.isBefore(endDateTime) && !now.isBefore(effectiveStartTime)
    }
    
    // 4. 分别处理日程和取件码
    val (pickupEvents, scheduleEvents) = activeEvents.partition { it.tag == EventTags.PICKUP }
    
    // 5. 聚合模式处理
    if (aggregateMode) {
        capsules.add(aggregatePickupCapsule)
    }
    
    return if (capsules.isNotEmpty()) CapsuleUiState.Active(capsules) else CapsuleUiState.None
}
```

当用户关闭实况胶囊时，OCR 进度/结果将回落为普通通知（POPUP 通道）。

### 4.3.3 胶囊提供者

应用实现了 ICapsuleProvider 接口来兼容不同系统：

```kotlin
interface ICapsuleProvider {
    fun buildNotification(context: Context, item: CapsuleItem, iconResId: Int): Notification
}
```

- **NativeCapsuleProvider**：原生 Android 14+ Live Activity
- **FlymeCapsuleProvider**：Flyme 系统实况通知（使用 RemoteViews）

### 4.3.4 聚合取件胶囊

当用户开启取件码聚合模式且同一天有多个活跃取件码时：

1. 所有独立取件码胶囊合并为一个聚合胶囊
2. 聚合胶囊显示取件码数量和品牌列表
3. 点击"已取"按钮会批量完成所有活跃取件码事件
4. 聚合胶囊消失后，其他胶囊不再显示

```kotlin
val aggregateMode = settings.isPickupAggregationEnabled && pickupEvents.size > 1
```

### 4.3.5 全局提前提醒功能

全局提前提醒允许所有日程在开始前显示胶囊和通知：

**设置选项**：

```kotlin
val isAdvanceReminderEnabled: Boolean = false  // 全局提前提醒总开关
val advanceReminderMinutes: Int = 30           // 提前分钟数（30/45/60）
```

### 4.3.6 动态标题实现

胶囊和悬浮窗根据日程类型显示不同的动态标题：

**TransportUtils.kt - 火车票解析**：

```kotlin
fun parseTrain(description: String, isCheckedIn: Boolean): TransportInfo {
    val parts = description.removePrefix("【列车】").split("|")
    val trainNo = parts.getOrNull(0) ?: ""
    val gate = parts.getOrNull(1) ?: ""
    val seat = parts.getOrNull(2) ?: ""
    
    // 动态标题：未检票显示检票口，已检票显示座位号
    val mainDisplay = if (isCheckedIn) seat else if (gate.isNotBlank()) "${gate} 检票" else "等待检票"
    
    return TransportInfo(
        type = TransportType.TRAIN,
        mainDisplay = mainDisplay,
        subDisplay = trainNo,
        isCheckedIn = isCheckedIn,
        rawDescription = description
    )
}
```

**TransportUtils.kt - 网约车解析**：

```kotlin
// v1.2.1 修复：正确解析颜色、车型、车牌三个字段
fun parseRide(description: String, isRideCompleted: Boolean = false): TransportInfo {
    val parts = description.removePrefix("【用车】").split("|")
    
    val mainDisplay = when {
        parts.size >= 3 -> {
            // 格式：颜色|车型|车牌
            val color = parts[0].ifBlank { "" }
            val carModel = parts[1].ifBlank { "" }
            val licensePlate = parts[2].ifBlank { "" }
            
            // 动态标题：未开始显示车牌，已完成显示已用车
            if (isRideCompleted) "(已用车)" else licensePlate
        }
        parts.size >= 2 -> {
            // 兼容旧格式
            val carModel = parts[0].ifBlank { "" }
            val licensePlate = parts[1].ifBlank { "" }
            if (isRideCompleted) "(已用车)" else licensePlate
        }
        else -> ""
    }
    
    val subDisplay = if (parts.size >= 3) {
        "${parts[0]} ${parts[1]}"  // 颜色 + 车型
    } else ""
    
    return TransportInfo(
        type = TransportType.TAXI,
        mainDisplay = mainDisplay,
        subDisplay = subDisplay,
        isCheckedIn = isRideCompleted,
        rawDescription = description
    )
}
```

**动态标题显示规则**：

| 类型 | 未开始/未检票 | 已开始/已检票/已完成 |
|------|---------------|---------------------|
| 普通日程 | 原标题 | 原标题 |
| 火车票 | "8A 检票" | "15A" (座位) |
| 网约车 | "鄂J·DB7979" | "(已用车)" |
| 取件码 | "5566" | 原标题 |

### 4.3.7 网速胶囊优化

**v1.2.1 更新**：优化网速胶囊显示格式，去掉↑符号并优化数值显示。

**优化内容**：

1. 去掉 ↓ 符号，节省字符空间
2. 添加单位进位逻辑：999KB/s → 1.0MB/s

```kotlin
private fun formatSpeed(bytesPerSecond: Long): String {
    return when {
        bytesPerSecond < 1024 -> "${bytesPerSecond}B/s"
        bytesPerSecond < 1024 * 1000 -> "${bytesPerSecond / 1024}KB/s"
        bytesPerSecond < 1024 * 1024 -> String.format("%.1fMB/s",
            bytesPerSecond / (1024.0 * 1024.0))
        else -> String.format("%.1fGB/s",
            bytesPerSecond / (1024.0 * 1024.0 * 1024.0))
    }
}
```

**显示效果对比**：

| 原网速 | 修改后 | 字符数 |
|--------|--------|--------|
| `↓ 512 B/s` | `512B/s` | 6 |
| `↓ 999 KB/s` | `999KB/s` | 7 |
| `↓ 1000 KB/s` | `1.0MB/s` | 6 |
| `↓ 1.5 MB/s` | `1.5MB/s` | 6 |
| `↓ 1.23 GB/s` | `1.2GB/s` | 6 |

## 4.4 悬浮窗功能

悬浮窗是应用的重要功能，允许用户在任何时候快速查看和操作日程。

### 4.4.1 触发方式

- **音量键触发**：长按音量+键（需要无障碍服务）
- **快捷设置磁贴**：点击下拉状态栏的快捷图标

### 4.4.2 智能标题

悬浮窗根据日程类型显示不同的智能标题：

| 日程类型 | 显示内容 |
|----------|---------|
| 火车票 | 检票口 / 座位号 |
| 网约车 | 车牌号 |
| 取件码 | 取件号码 |
| 普通日程 | 原始标题 |

### 4.4.3 滑动操作

悬浮窗支持左滑快捷操作：
- **左滑**：完成事件（已取件/已检票）
- **右滑**：关闭悬浮窗

## 4.5 日历同步

应用支持与系统日历进行双向同步。

### 4.5.1 同步策略

- 默认双向同步：本地变更触发 App → 系统日历，系统日历变更通过 `CalendarContentObserver` 与定时同步回流到应用（1 分钟周期）。
- 同步窗口：前后 30 天范围内的事件。
- 过滤规则：仅同步 `eventType == EVENT` 且 `skipCalendarSync == false` 的事件，重复日程默认不回流。
- 课程同步为单向强制重建，仅同步未来 16 周课程实例。
- 低内存保护：可用内存低于 64MB 时中断同步并写入异常日志。

### 4.5.2 重复日程同步（Beta）

- 开关 `isRecurringCalendarSyncEnabled` 用于控制重复日程回流。
- 仅同步 ±30 天实例，且限制最大实例数为 2000。
- 超过上限会终止同步并自动回滚开关。
- 关闭开关时会清理已导入的重复实例。

### 4.5.3 去重机制

同步时使用事件指纹（EventFingerprint）进行去重：

```kotlin
data class EventFingerprint(
    val title: String,
    val startDate: LocalDate,
    val endDate: LocalDate,
    val startTime: String,
    val endTime: String,
    val location: String
)
```

## 4.6 其他功能

### 4.6.1 每日汇总通知

在设定的时间推送当日日程概要：
- 早间汇总：播报今日日程安排
- 晚间汇总：播报明日日程预览

### 4.6.2 自动归档

过期日程自动归档，减少主列表干扰：
- 可配置过期天数阈值
- 归档后仍可在归档页查看

### 4.6.3 智能推荐

基于历史数据推荐合适的日程安排时间：

```kotlin
class ScheduleRecommendationAnalyzer {
    fun analyze(events: List<MyEvent>): List<TimeRecommendation>
}
```

### 4.6.4 异常日志与 ANR 监测

- CrashHandler 捕获未处理异常并写入 `/Download/CrashLogs/exception.log`（单文件追加、日切清理、512KB 上限）。
- AnrMonitor 监测主线程卡顿（>5s）并记录简要堆栈；日历同步失败与低内存预警也会写入异常日志。

---

# 第五部分：数据模型

## 5.1 核心数据模型

### MyEvent（用户日程）

```kotlin
data class MyEvent(
    val id: String = UUID.randomUUID().toString(),
    val title: String,                             // 标题
    val startDate: LocalDate,                      // 开始日期
    val endDate: LocalDate,                        // 结束日期
    val startTime: String,                         // 开始时间 HH:mm
    val endTime: String,                           // 结束时间 HH:mm
    val location: String,                          // 地点
    val description: String,                        // 描述（可含微格式）
    val color: Color,                              // 颜色
    val isImportant: Boolean = false,              // 是否重要
    val sourceImagePath: String? = null,           // 来源截图路径
    val reminders: List<Int> = emptyList(),        // 提醒分钟数列表
    val eventType: String = EventType.EVENT,       // 事件类型
    val tag: String = EventTags.GENERAL,          // 标签
    val isCompleted: Boolean = false,              // 是否已完成
    val completedAt: Long? = null,                 // 完成时间戳
    val originalEndDate: LocalDate? = null,        // 原始结束日期
    val originalEndTime: String? = null,           // 原始结束时间
    val isCheckedIn: Boolean = false,              // 是否已签到
    val archivedAt: Long? = null,                  // 归档时间戳
    val lastModified: Long = System.currentTimeMillis(), // 最后修改时间
    val isRecurring: Boolean = false,              // 是否重复日程
    val isRecurringParent: Boolean = false,        // 是否重复父事件
    val recurringSeriesKey: String? = null,        // 重复系列键
    val recurringInstanceKey: String? = null,      // 重复实例键
    val parentRecurringId: String? = null,         // 父事件 ID
    val excludedRecurringInstances: List<String> = emptyList(), // 排除实例
    val nextOccurrenceStartMillis: Long? = null,   // 下次发生时间
    val skipCalendarSync: Boolean = false          // 是否跳过日历同步
)
```

### EventType（事件类型常量）

```kotlin
object EventType {
    const val EVENT = "event"    // 普通日程（含取件码、火车、打车）
    const val COURSE = "course"   // 课程
}
```

### EventTags（事件标签常量）

```kotlin
object EventTags {
    const val GENERAL = "general"  // 普通日程、会议、约会
    const val PICKUP = "pickup"    // 取件、取餐、核销码
    const val TRAIN = "train"      // 火车、高铁、飞机
    const val TAXI = "taxi"        // 网约车、出租车
}
```

### MySettings（用户设置）

```kotlin
data class MySettings(
    // AI 模型配置
    val modelKey: String = "",
    val modelName: String = "gpt-3.5-turbo",
    val modelUrl: String = "",
    val modelProvider: String = "", // 兼容旧字段
    val useMultimodalAi: Boolean = false,
    val mmModelKey: String = "",
    val mmModelName: String = "",
    val mmModelUrl: String = "",
    
    // 功能开关
    val showTomorrowEvents: Boolean = false,
    val isDailySummaryEnabled: Boolean = false,
    val isAdvanceReminderEnabled: Boolean = false,
    val advanceReminderMinutes: Int = 30,
    
    // 识别设置
    val tempEventsUseRecognitionTime: Boolean = true,
    val screenshotDelayMs: Long = 1000L,
    val isLiveCapsuleEnabled: Boolean = false,
    
    // 取件码聚合开关
    val isPickupAggregationEnabled: Boolean = false,
    
    // 归档配置
    val autoArchiveEnabled: Boolean = false,
    val archiveDaysThreshold: Int = 0,
    
    // 课表设置
    val semesterStartDate: String = "",
    val totalWeeks: Int = 20,
    val timeTableJson: String = "",
    val timeTableConfigJson: String = "",
    
    // 主题设置
    val isDarkMode: Boolean = false,
    val themeMode: Int = 1,           // 1=系统 2=浅色 3=深色
    val themeColorScheme: String = "DEFAULT",
    val uiSize: Int = 2,              // 1=小 2=中 3=大
    
    // 实验室功能
    val isNetworkSpeedCapsuleEnabled: Boolean = false,
    val enableSmartRecommendation: Boolean = true,
    val isFloatingWindowEnabled: Boolean = false,
    val isRecurringCalendarSyncEnabled: Boolean = false
)
```

### Course（课程）

```kotlin
data class Course(
    val id: String,
    val name: String,
    val location: String = "",
    val teacher: String = "",
    val color: Color,
    val dayOfWeek: Int,
    val startNode: Int,
    val endNode: Int,
    val startWeek: Int,
    val endWeek: Int,
    val weekType: Int = 0,
    val excludedDates: List<String> = emptyList(),
    val isTemp: Boolean = false,
    val parentCourseId: String? = null
)
```

### TimeNode（作息时间节点）

```kotlin
data class TimeNode(
    val index: Int,                       // 节次序号
    val startTime: String,                // 开始时间 HH:mm
    val endTime: String,                  // 结束时间 HH:mm
    val period: String = ""              // 分段：morning/afternoon/night
)
```

---

# 第六部分：服务层

## 6.1 服务概览

应用包含多个后台服务，分别负责不同的系统级功能：

| 服务 | 用途 | 运行模式 |
|------|------|---------|
| CapsuleService | 实况胶囊显示 | 前台服务 |
| FloatingScheduleService | 悬浮窗显示 | 前台服务 |
| TextAccessibilityService | 截屏无障碍 | 系统服务 |
| CaptureTileService | 快捷磁贴 | 系统服务 |

## 6.2 广播接收器

| 接收器 | 用途 |
|--------|------|
| AlarmReceiver | 闹钟提醒触发 |
| BootReceiver | 开机自启动 |
| DailySummaryReceiver | 每日汇总通知 |
| CalendarSyncReceiver | 定期日历同步 |
| EventActionReceiver | 事件操作（已取/延长） |

## 6.3 权限清单

```xml
<!-- 基础权限 -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />

<!-- 闹钟权限 -->
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
<uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

<!-- 前台服务 -->
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

<!-- 开机自启 -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

<!-- 悬浮窗 -->
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

<!-- Flyme 专用 -->
<uses-permission android:name="flyme.permission.READ_NOTIFICATION_LIVE_STATE" />

<!-- 日历权限 -->
<uses-permission android:name="android.permission.READ_CALENDAR" />
<uses-permission android:name="android.permission.WRITE_CALENDAR" />
```

---

# 第六部分：用户操作手册

## 6.1 基础导航

### 6.1.1 主界面布局

应用采用底部导航栏设计，包含三个主要入口：

- **主页**：显示当前选中日期的日程列表和课程卡片
- **全部日程**：显示所有日程的列表视图，支持搜索和筛选
- **设置**：AI 模型设置、课程表设置、偏好设置、数据备份

主页采用上下分栏设计：上半部分显示日期卡片（含星期/农历），下半部分显示当日日程列表。向下滑动可以展开课程表视图。

### 6.1.2 设置页面导航

设置页面采用侧边栏导航，点击底部导航栏的设置图标进入。侧边栏包含以下入口：

- 课表管理
- 作息表管理
- 学期配置
- AI 模型
- 偏好设置
- 数据备份
- 实验室
- 主题设置
- 关于

## 6.2 手势操作

### 6.2.1 下拉查看课表

**操作方式**：在主页视图中，向下拖动屏幕

**效果**：

- 课程表视图从底部逐渐浮现
- 当前日程列表向上滑动并淡出
- 课程表呈现缩放放大的动画效果

### 6.2.2 日期切换

**操作方式**：在主页视图的日期卡片上左右滑动

### 6.2.3 事件项操作菜单

**操作方式**：在日程项上向左滑动

**操作菜单包含三个按钮**：

- 编辑（绿色）：修改日程内容
- 星标（黄色）：标记为重要
- 删除（红色）：删除日程

### 6.2.4 悬浮窗左滑操作

**操作方式**：在悬浮窗的事件项上向左滑动

**操作按钮**：

- 已完成（绿色）：标记日程为已完成
- 已取件（蓝色）：标记取件码为已取
- 已检票（橙色）：标记火车票为已检票
- 已用车（紫色）：标记网约车为已用车

## 6.3 课程表功能

### 6.3.1 查看课程表

1. 在主页视图中向下拉动屏幕
2. 课程表从底部浮现
3. 可查看整周的课程安排

### 6.3.2 周次切换

- 点击左右箭头切换周次
- 点击周次标题快速返回本周

### 6.3.3 添加课程

**入口**：设置 → 课表管理 → 管理课程 → 添加课程

**可设置字段**：

- 课程名称（必填）
- 上课地点
- 授课教师
- 星期几
- 第几节到第几节
- 周次范围（第几周到第几周）
- 课程频率（每周/单周/双周）
- 颜色（自动分配）

### 6.3.4 导入课程

**入口**：设置 → 课表管理 → 导入课程表

**支持格式**：醒课表数据格式

## 6.4 日程管理

### 6.4.1 添加日程

**方式一：手动添加**

1. 点击右下角悬浮按钮（+）
2. 填写日程信息
3. 设置提醒时间
4. 点击确定

**方式二：AI 文本创建（悬浮窗）**

1. 开启“悬浮日程”功能并长按音量+呼出悬浮窗
2. 在底部输入框输入自然语言描述
3. 点击发送或键盘完成

### 6.4.2 智能识别

**方式一：快捷设置磁贴**

1. 开启无障碍服务
2. 下拉快捷设置，点击"识别日程"磁贴
3. 等待延迟后自动截屏
4. AI 自动识别日程信息

**方式二：一键识屏快捷方式**

1. 开启无障碍服务
2. 长按桌面添加快捷方式，选择"一键识屏"
3. 设置侧滑手势触发该快捷方式
4. 侧滑触发后自动截屏识别

**方式三：图片导入识别**

1. 在主页展开 FAB，点击“识别图片”按钮，或在悬浮窗点击图片按钮
2. 选择相册图片
3. OCR + AI 自动识别日程信息

## 6.5 设置说明

### 6.5.1 AI 模型设置

**可配置项**：

- 模式切换：文本 AI / 多模态 AI
- 文本 AI：模型名称 / API 地址 / API Key
- 多模态 AI：模型名称 / API 地址 / API Key（支持图片输入的模型）

### 6.5.2 偏好设置

**显示板块**：

- 界面大小：调整字体大小
- 显示明日日程：在今日日程列表底部预览明日安排

**AI 板块**：

- 使用多模态 AI：开启后图片识别切换为多模态模型

**通知板块**：

- 每日日程提醒：早06:00和晚22:00推送汇总
- 实况胶囊通知：日程开始前显示胶囊
- 取件码聚合：当有多个取件码时合并显示
- 日程提前提醒：可设置提前 30/45/60 分钟

**日程板块**：

- 日历同步：将课程和日程同步到系统日历
- 同步重复日程（Beta）：仅同步 ±30 天实例
- 自动归档：日程过期后自动归档
- 智能推荐：创建日程时显示推荐

**截图板块**：

- 截图延迟：1000–2500ms，可调 OCR 触发等待

### 6.5.3 实验室功能

- 网速胶囊开关
- 悬浮日程开关

---

# 第七部分：技术实现

## 7.1 第三方依赖

### 生产依赖

| 库名 | 用途 | 版本 |
|------|------|------|
| Jetpack Compose BOM | UI 框架 | 2024.09.00 |
| Material3 | UI 组件库 | 1.5.4 |
| Ktor Client | HTTP 客户端 | 2.3.7 |
| ML Kit Text Recognition | 文字识别 | 16.0.1 |
| Kotlinx Serialization | JSON 序列化 | 1.6.0 |
| Navigation Compose | 页面导航 | 2.8.0 |
| Lifecycle Runtime KTX | 状态管理 | 2.10.0 |
| Lifecycle ViewModel Compose | Compose ViewModel | 2.8.7 |
| Core KTX | 核心扩展 | 1.17.0 |
| Desugar JDK Libs | Java 8+ 兼容 | 2.0.4 |
| Material Icons Extended | 图标库 | 1.5.4 |

### 测试依赖

| 库名 | 用途 |
|------|------|
| JUnit | 单元测试 |
| Espresso | 集成测试 |
| Compose UI Test | UI 测试 |

## 7.2 权限清单

### 必须权限

| 权限 | 用途 |
|------|------|
| INTERNET | AI API 调用 |
| POST_NOTIFICATIONS | 发送通知（Android 13+） |
| POST_PROMOTED_NOTIFICATIONS | 推广通知 |
| SCHEDULE_EXACT_ALARM | 调度精确闹钟 |
| USE_EXACT_ALARM | 使用精确闹钟 |
| USE_FULL_SCREEN_INTENT | 全屏闹钟提示 |
| FOREGROUND_SERVICE | 前台服务（胶囊） |
| FOREGROUND_SERVICE_SPECIAL_USE | 特殊用途前台服务 |
| RECEIVE_BOOT_COMPLETED | 开机启动 |
| SYSTEM_ALERT_WINDOW | 悬浮窗 |

### 可选权限

| 权限 | 用途 |
|------|------|
| READ_CALENDAR | 读取系统日历 |
| WRITE_CALENDAR | 写入系统日历 |
| com.android.alarm.permission.SET_ALARM | 设置系统闹钟 |
| flyme.permission.READ_NOTIFICATION_LIVE_STATE | Flyme 胶囊支持 |

## 7.3 通知渠道

| 渠道 ID | 名称 | 重要性 | 用途 |
|---------|------|--------|------|
| calendar_assistant_popup_channel_v2 | 普通通知 | HIGH | 日程提醒通知 |
| calendar_assistant_live_channel_v3 | 实况胶囊 | HIGH | 实况胶囊（无声高优） |

## 7.4 状态栏图标资源

**v1.2.1 新增**：应用新增了多个状态栏图标，用于在通知栏显示业务状态。

| 文件 | 用途 | 图标描述 |
|------|------|----------|
| `ic_stat_scan.xml` | 识别中 | 扫描框 📷 |
| `ic_stat_sparkle.xml` | 分析完成 | 星星 ✨ |
| `ic_stat_success.xml` | 添加成功 | 勾选 ✓ |
| `ic_stat_package.xml` | 快递取件 | 包裹 📦 |
| `ic_stat_food.xml` | 餐饮取餐 | 汉堡 🍔 |
| `ic_stat_train.xml` | 火车出行 | 火车 🚄 |
| `ic_stat_car.xml` | 打车出行 | 汽车 🚗 |
| `ic_stat_net.xml` | 网速胶囊 | 网络 |

### 胶囊图标资源

| 文件 | 用途 |
|------|------|
| `ic_capsule_train.xml` | 火车胶囊 |
| `ic_capsule_taxi.xml` | 打车胶囊 |
| `ic_capsule_pickup.xml` | 取件胶囊 |
| `ic_capsule_event.xml` | 日程胶囊 |

## 7.5 数据存储

| 数据类型 | 存储位置 | 格式 |
|---------|----------|------|
| 日程事件 | filesDir/events.json | JSON |
| 课程数据 | filesDir/courses.json | JSON |
| 归档事件 | filesDir/archives.json | JSON |
| 同步数据 | filesDir/sync_data.json | JSON |
| 应用设置 | SharedPreferences | Key-Value |
| 导出备份 | Downloads/CalendarBackup_*.json | JSON |
| 异常日志 | Downloads/CrashLogs/exception.log | LOG |

## 7.6 UI 常量

| 常量 | 值 | 说明 |
|------|------|------|
| HeaderHeight | 50.dp | 课表表头高度 |
| SidebarWidth | 35.dp | 节次栏宽度 |
| NodeHeight | 64.dp | 单节课程高度 |
| DragThreshold | 200.dp | 下拉展开阈值（1/3屏幕） |
| VelocityThreshold | 1000f | 快速滑动阈值 |
| SwipeMenuWidth | 160.dp | 侧滑菜单宽度 |
| ScreenshotDelay | 1000ms | 截图默认延迟 |

---

# 第八部分：版本历史

## v1.2.1 (2026-03-13)

### 新增功能
- **OCR 进度/结果胶囊**：识别过程纳入实况胶囊体系，结束后自动恢复事件胶囊
- **多模态 AI 模式**：图片直传统一 Prompt（支持视觉模型）
- **异常日志/ANR 监测**：写入 `/Download/CrashLogs/exception.log`，日切清理
- **动态状态栏图标**：根据识别状态和事件类型显示不同图标
- **云端 Prompt 更新**：支持从云端更新识别规则（含 `mm_unified_prompt`）

### Bug 修复
- **作息表起始时间回显**：引入 `timeTableConfigJson` 作为锚点
- **OCR 通知回落逻辑**：未开启实况胶囊时回退普通通知
- **重复日程同步保护**：实例数量超过上限自动终止并回滚开关

### UI 优化
- **截图延迟范围**：默认 1000ms，范围 1000–2500ms

---

## v1.2.0 (2026-03-10)

- 取件码聚合模式
- 网速胶囊功能

---

## v1.1.0 (2026-03-08)

- 悬浮窗功能增强
- 智能标题显示

---

## v1.0.0 (2026-03-05)

- 初始稳定版本

---

**文档结束**

本文档由项目源码自动生成，最后更新于 2026-03-12。
